spring.application.name=back

# Puerto del servidor
server.port=8081

# Configurar la información de nuestra DB MySQL (url, username, password) - Spring boot se encarga de crear la bd y las tablas
spring.datasource.url=jdbc:mysql://localhost:3306/amanalli?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=S0p0rt3$123

# Establecer la acción DDL con JPA DEFINIR LA ESTRUCTURA DE LA BASE DE DATOS
# Proveedor hibernate del jpa JPA pasa la información de las API  al formato de las tablas de bases de datos.
spring.jpa.hibernate.ddl-auto=update

# Config para visualizar lo que JPA realiza solo es visual para usar durante el desarrollo
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# path configuration
server.servlet.context-path=/api/v1